# 前言

## 为什么写这本书

我第一次对 Node.js 产生"不对劲"的感觉，是在排查一次线上事故的时候。

一台服务器上的 Node 进程突然拒绝所有新连接，日志里刷满 `EMFILE: too many open files`。重启进程，恢复；几小时后，复发。当时我对 Node.js 的理解停留在 API 层面——我知道 `fs.readFile` 怎么用，知道 `http.createServer` 怎么写，但我回答不了一个最基本的问题：**"打开的文件"和"网络连接"，为什么会消耗同一种资源？**

顺着这个问题往下挖，我发现自己掉进了一个越来越深的洞：文件描述符是什么？为什么 socket 也是文件？内核是怎么看待这些东西的？再往下，问题变成了：Node.js 的单线程到底是什么意思？事件循环凭什么能让一个线程服务上万连接？`emit` 到底是同步还是异步？

等我从洞里爬出来，我意识到一件事：**Node.js 的所有机制不是并列的知识点，而是一条因果链**。单线程这个约束在链条的最顶端，往下每一环都是对上一环遗留问题的回答。理解了这条链，所有"为什么"都有了答案；不理解这条链，背下再多 API 也只是知识的散件。

这本书就是那条因果链的完整记录。

## 这本书写给谁

你适合读这本书，如果你：

- 用 Node.js 写过实际项目（一年以上经验），对 `require`、`Promise`、`stream.pipe` 这些概念不陌生；
- 更想知道"为什么是这样"，而不是"应该怎么写"；
- 遇到过一些解释不了的现象——比如 `setImmediate` 和 `setTimeout(fn, 0)` 谁先执行时快时慢，比如大文件下载把内存打爆，比如一个未处理的 `error` 事件让整个进程消失。

你暂时不适合读这本书，如果你还没写过 Node.js 程序——请先去写，遇到问题再回来，这本书的每一章都在回答"用的时候想不通"的问题。

## 这本书不讲什么

本书刻意不覆盖：Express/Koa 等框架、npm 生态、TypeScript、测试、部署运维，也不做 API 手册式的参数罗列。crypto、HTTP/2、WASI 这类子系统只在附录给出定位——它们是建立在本书七个核心机制之上的"应用"，理解了地基，上层建筑可以按需自学。

## 全书的骨架

全书四部十三章，是一条单向的推导链：

**第一部（约束）**：一切从"JS 在 V8 里单线程执行"开始。第 1 章讲这个约束的来源与代价；第 2 章讲被 V8 沙箱关住的 JS 如何通过 Binding 拿到操作系统的能力。

**第二部（心跳）**：有了系统能力，单线程仍会被 I/O 等待卡死。第 3 章讲事件循环如何让"等待"消失；第 4 章讲两类 I/O（网络与磁盘）为什么走上了两条完全不同的路。

**第三部（血液）**：心脏泵的是数据。第 5 章讲 fd——内核用一个整数统一了文件、socket 和管道；第 6 章讲 Buffer——JS 与内核交换字节的唯一容器；第 7 章讲 Stream——如何用有限内存处理无限数据；第 8 章讲 EventEmitter——所有事件的分发引擎。

**第四部（容器）**：零件如何组装成整体。第 9 章讲运行时容器 Environment；第 10 章讲从启动到退出的生命周期；第 11 章讲单线程如何通过"复制容器"吃满多核；第 12 章讲 async_hooks 与 AsyncLocalStorage 如何让"来龙去脉"穿过异步回调、线程与进程的边界。

**终章**把十二章的机制串成一个 HTTP 请求的完整旅程——如果你读完终章能在脑中完整放映这部"电影"，这本书的目的就达到了。

## 阅读约定

- 每章开篇有**本章问题**，来自上一章结尾的"未解之题"；结尾有**本质小结**（一句话本质 + 要点清单）和**下一章引子**。
- 代码示例刻意保持最小；架构图使用字符图，重要的是结构关系而非视觉效果。
- 涉及源码时只给出文件定位（如 `src/env.cc`）与关键函数名，不粘贴大段源码——源码会过时，机制不会。
- 内核相关的讨论以 Linux 为基准（epoll），macOS（kqueue）与 Windows（IOCP）的差异只在必要处说明。

好了。让我们从那个最初的约束开始。

---

[开始阅读：第 1 章 单线程的枷锁与馈赠 →](./part-1-constraint/ch01-single-thread.md)
